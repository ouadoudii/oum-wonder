{
  "name": "oum-wonder",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf public && mkdir -p public",
    "build": "npm run clean && tsc && node scripts/bundle.mjs && cp index.html styles.css public/",
    "typecheck": "tsc --noEmit",
    "lint": "node scripts/lint.mjs",
    "test": "npm run build && node --test tests/unit/*.test.mjs",
    "test:e2e": "npm run build && python3 tests/e2e/test_app.py && python3 tests/e2e/test_export.py && python3 tests/e2e/test_spatial_light.py && python3 tests/e2e/test_room_measurements.py && python3 tests/e2e/test_room_constraints.py",
    "audit": "node scripts/audit-deps.mjs",
    "quality": "npm run typecheck && npm run lint && npm run test && npm run audit && npm run build && npm run test:e2e"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
